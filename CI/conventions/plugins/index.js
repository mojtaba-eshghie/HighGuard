/**
 * This index.js file will be called by other parts of the monitor (such as run.js or tests).
 * The practice is to put plugin conventions as separate js files under conventions/plugins. 
 * Import them here in this index file, then, execute the ones that are passed to plugins index 
 * file as parameters.
 */
