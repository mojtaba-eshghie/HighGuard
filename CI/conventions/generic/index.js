require('module-alias/register');
//const { constructor } = require('@lib/conventions/generic/constructor');
/**
 * This index.js file will be called by other parts of the monitor (such as run.js or tests).
 * The practice is to put generic conventions as separate js files under conventions/generic. 
 * Import them here in this index file, then, execute them accordingly (possibly with their own custom
 * parameters and configurations). 
 */
